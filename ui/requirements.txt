# # python -m venv venv
# # venv\Scripts\activate

# # Install torch & torchvision separately:
# # For Windows, install torch/torchvision using the official PyTorch install selector
# # CPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# # CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# streamlit>=1.20.0
# streamlit-webrtc>=0.44.0
# av>=10.0.0
# opencv-python>=4.6.0
# numpy>=1.24.0
# timm>=0.6.12
# Pillow>=9.0.0

# # - `streamlit-webrtc` requires `aiortc` and system ffmpeg for full realtime support.

# ===========================================================================

# 1. Uninstall the conflicting "old" stack
# pip uninstall -y numpy mediapipe opencv-python opencv-contrib-python jax jaxlib

# 2. Install the new "modern" stack
# pip install -r requirements.txt

# Modern Stack (Python 3.10 - 3.13 compatible)
numpy>=2.0
opencv-python>=4.12
opencv-contrib-python>=4.12
jax>=0.4.35
jaxlib>=0.4.35

# AI & App
torch
torchvision
torchaudio
timm
onnxruntime
streamlit
av
streamlit-webrtc